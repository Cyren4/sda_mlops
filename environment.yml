name: banking-mlops
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.10
  - pip
  - pip:
      - torch 
      - numpy 
      - pandas
      - matplotlib
      - seaborn 
      - scikit-learn 
      - pyyaml 
      - tqdm 
      - transformers
      - mlflow
      - jinja2
      - flask
      - setuptools
      - streamlit
      - graphviz
  - brew: 
      - graphviz
